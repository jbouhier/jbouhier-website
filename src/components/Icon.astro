---
import { Icon as AstroIcon } from 'astro-icon/components'

interface Props {
  icon: string
  class?: string
}

const { icon, class: className = '' } = Astro.props
const iconClass = `w-6 h-6 ${className}`
---

<AstroIcon name={icon} class={iconClass} />
